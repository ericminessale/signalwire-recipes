# generated — regenerate with `python build.py`
site/

__pycache__/
*.pyc
.env
.venv/
node_modules/

# seeded design canvas (rebuild with seed-canvas.mjs)
design/signalwire-recipes-directions.html

# codex review session logs (answers are saved to docs/*_REVIEW_*.md)
docs/_sol_*.log

# playwright-cli session logs and snapshots
.playwright-cli/

# internal planning material stays local: briefs, reviews, handoffs, marketing
# inputs, competitive enumeration. The shareable list is docs/INVENTORY.md;
# inventory.json and its renderer stay because scaffold.py seeds from them.
docs/*
!docs/INVENTORY.md
!docs/team/
!docs/enum/
docs/enum/*
!docs/enum/inventory.json
!docs/enum/render_inventory.py

# abandoned design exploration from an early session; kept locally
design/

# the deploy build venv (tools/vercel_build.sh)
.vbuild/

# the hand-off recipe's local notes store, and its verifier's scratch copy
handoff-notes.json
.verify-notes.json

# the tenant provisioning recipe: a local store holding live tenant tokens
tenants.json

# a research agent unpacked an npm tarball here once
package/
*.tgz
