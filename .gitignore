# generated — regenerate with `python build.py`
site/

__pycache__/
*.pyc
.env
.venv/
node_modules/

# Local assistant instructions and working state are not repository content.
/CLAUDE.md
/claude.md
/CLAUDE.local.md
/claude.local.md
/AGENTS.md
/agents.md
/AGENTS.local.md
/agents.local.md
/GEMINI.md
/gemini.md
/.mcp.json
/.aider*
/.claude/
/.codex/
/.continue/
/.cursor/

# Internal roadmap, review material, and the roadmap-driven scaffolder stay local.
/docs/
/scaffold.py

# seeded design canvas (rebuild with seed-canvas.mjs)
design/signalwire-recipes-directions.html

# Browser test session logs and snapshots
.playwright-cli/

# abandoned design exploration from an early session; kept locally
design/

# the deploy build venv (tools/vercel_build.sh)
.vbuild/

# the hand-off recipe's local notes store, and its verifier's scratch copy
handoff-notes.json
.verify-notes.json

# the tenant provisioning recipe: a local store holding live tenant tokens
tenants.json

# a research agent unpacked an npm tarball here once
package/
*.tgz

# compiled output of a typescript surface
recipes/*/typescript/dist/

# the SMS survey recipe: per-number progress, a local stand-in for your table
survey-state.json
